components: []
container:
  properties:
    html: |-
      <!-- <center style="font-style:italic; color:#888; margin: 7px;"> -->
      <div class="progress-bar-component-wrapper">
        <div class="progress-container">
          <div class="progress-bar-negative" anvil-if="self.value < 0" anvil-name="progress-bar-negative"></div>
          <div class="progress-bar-positive" anvil-if="self.value >= 0" anvil-name="progress-bar-positive"></div>
          <div class="progress-label" anvil-name="progress-value-label"></div>
        </div>
      </div>
      <!-- </center> -->
      <div anvil-slot="default"></div>

      <!-- <script>
        function updateBarWidths(negPercent, posPercent) {
          var negBar = document.querySelector('.progress-bar-negative');
          var posBar = document.querySelector('.progress-bar-positive');
          negBar.style.width = negPercent + '%';
          posBar.style.width = posPercent + '%';
        }
      </script> -->
  type: HtmlTemplate
custom_component: true
events: []
is_package: true
properties:
- {default_binding_prop: true, default_value: 0, description: The progress shown on the bar, important: true, name: value, type: number}
- {default_value: null, important: true, name: min_value, type: number}
- {default_value: null, important: true, name: max_value, type: number}
